#!/bin/sh
# Pre-commit hook: run freshness gate
# Install with: git config core.hooksPath .githooks

set -euo pipefail

echo "githooks: running cargo xtask check..."
cargo xtask check

echo "githooks: check passed"
